SELECT S1.PRODUCT_ID,S1.YEAR AS FIRST_YEAR,S1.QUANTITY,S1.PRICE FROM SALES S1 INNER JOIN (SELECT PRODUCT_ID,MIN(YEAR) AS YEAR FROM SALES GROUP BY PRODUCT_ID) S2 ON S1.PRODUCT_ID=S2.PRODUCT_ID AND S1.YEAR=S2.YEAR